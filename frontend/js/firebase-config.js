// Firebase 콘솔 > 프로젝트 설정 > 일반 > 내 앱(웹)에서 확인한 값으로 아래를 채워주세요.
// 이 값들은 클라이언트에 노출되어도 되는 식별자입니다(비밀키가 아님).
// 실제 접근 제어는 firestore.rules 로 처리합니다.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
